# Google Custom Search API Setup Guide

คู่มือการตั้งค่า Google Custom Search API สำหรับ Branded Search Ranking และ Keyword Discovery

## 📋 สิ่งที่ต้องมี

1. บัญชี Google (Gmail)
2. Google Cloud Console access
3. Custom Search Engine

---

## 🚀 ขั้นตอนการตั้งค่า

### ขั้นตอนที่ 1: สร้าง Custom Search Engine (Search Engine ID)

1. ไปที่ [Google Programmable Search Engine](https://programmablesearchengine.google.com/)
2. คลิก **"Add"** หรือ **"Create a custom search engine"**
3. กรอกข้อมูล:
   - **Sites to search**: 
     - สำหรับการค้นหาแบบทั่วไป: ใส่ `*` (ดาว) เพื่อค้นหาทั้งอินเทอร์เน็ต
     - หรือใส่ domain เฉพาะ เช่น `example.com/*`
   - **Name of the search engine**: ตั้งชื่อ เช่น "SAO Auditor Search"
   - **Language**: เลือกภาษา (เช่น English หรือ Thai)
4. คลิก **"Create"**
5. คลิก **"Control Panel"** เพื่อเข้าสู่การตั้งค่า
6. ในหน้า Control Panel:
   - ไปที่ **"Setup"** → **"Basics"**
   - คัดลอก **"Search engine ID"** (CX) - ดูเหมือน: `978788761c749457a` หรือ `017576662512468239146:omuauf_lfve`
   - **หรือหาได้จาก script tag**: ถ้าคุณมี code `<script async src="https://cse.google.com/cse.js?cx=978788761c749457a">` แล้ว Search Engine ID คือ `978788761c749457a`
   - นี่คือ `GOOGLE_CUSTOM_SEARCH_ENGINE_ID` ของคุณ

### ขั้นตอนที่ 2: เปิดใช้งาน Custom Search API

1. ไปที่ [Google Cloud Console](https://console.cloud.google.com/)
2. สร้างโปรเจกต์ใหม่ (หรือใช้โปรเจกต์ที่มีอยู่):
   - คลิก **"Select a project"** → **"New Project"**
   - ตั้งชื่อโปรเจกต์ เช่น "SAO Auditor"
   - คลิก **"Create"**
3. เปิดใช้งาน Custom Search API:
   - ไปที่ [API Library](https://console.cloud.google.com/apis/library)
   - ค้นหา **"Custom Search API"**
   - คลิก **"Enable"**

### ขั้นตอนที่ 3: สร้างหรือใช้ API Key ที่มีอยู่

**สำคัญ**: Google Custom Search API และ Google PageSpeed API **ใช้ API Key เดียวกันได้** เพราะเป็น Google Cloud APIs ทั้งคู่

#### ตัวเลือก A: ใช้ API Key เดียวกับ PageSpeed (แนะนำ)

ถ้าคุณมี `GOOGLE_PAGESPEED_API_KEY` อยู่แล้ว:

1. ใช้ API Key เดียวกันสำหรับทั้งสอง API
2. ตรวจสอบว่า API Key นี้รองรับทั้งสอง APIs:
   - ไปที่ [Credentials](https://console.cloud.google.com/apis/credentials)
   - คลิกที่ API Key ของคุณ
   - ตรวจสอบว่า **"API restrictions"** รวม:
     - ✅ **PageSpeed Insights API**
     - ✅ **Custom Search API**
   - ถ้ายังไม่มี Custom Search API ให้เพิ่ม:
     - คลิก **"Edit API key"**
     - ใน **"API restrictions"** → เลือก **"Restrict key"**
     - เลือกทั้ง **"PageSpeed Insights API"** และ **"Custom Search API"**
     - คลิก **"Save"**

#### ตัวเลือก B: สร้าง API Key ใหม่

1. ไปที่ [Credentials](https://console.cloud.google.com/apis/credentials)
2. คลิก **"+ CREATE CREDENTIALS"** → **"API key"**
3. คัดลอก API Key ที่สร้างขึ้น (ดูเหมือน: `AIzaSyDaGmWKa7JsX8-CqWy3a1234567890`)
4. **สำคัญ**: คลิก **"RESTRICT KEY"** เพื่อจำกัดการใช้งาน:
   - **Application restrictions**: เลือก "None" หรือ "HTTP referrers" (ถ้าต้องการ)
   - **API restrictions**: เลือก "Restrict key" → เลือก:
     - ✅ **Custom Search API**
     - ✅ **PageSpeed Insights API** (ถ้าต้องการใช้ด้วย)
   - คลิก **"Save"**
5. นี่คือ API Key ที่ใช้ได้กับทั้งสอง APIs

### ขั้นตอนที่ 4: ตั้งค่าใน .env

1. เปิดไฟล์ `.env` ในโปรเจกต์ของคุณ
2. เพิ่มหรืออัปเดตบรรทัดต่อไปนี้:

#### วิธีที่ 1: ใช้ API Key เดียวกับ PageSpeed (แนะนำ)

```env
# ใช้ API Key เดียวกันสำหรับทั้งสอง APIs
GOOGLE_PAGESPEED_API_KEY=AIzaSyDaGmWKa7JsX8-CqWy3a1234567890
GOOGLE_CUSTOM_SEARCH_API_KEY=AIzaSyDaGmWKa7JsX8-CqWy3a1234567890

# Search Engine ID (ได้จาก Custom Search Engine)
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=978788761c749457a
```

**หมายเหตุ**: ใช้ค่า API Key เดียวกันสำหรับทั้ง `GOOGLE_PAGESPEED_API_KEY` และ `GOOGLE_CUSTOM_SEARCH_API_KEY`

#### วิธีที่ 2: ใช้ API Key แยกกัน

```env
GOOGLE_PAGESPEED_API_KEY=AIzaSy...your-pagespeed-key
GOOGLE_CUSTOM_SEARCH_API_KEY=AIzaSy...your-custom-search-key
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=978788761c749457a
```

3. แทนที่ค่าด้วย:
   - **API Key**: จาก Google Cloud Console (ใช้ตัวเดียวกับ PageSpeed หรือแยกกัน)
   - **Engine ID**: `978788761c749457a` (หรือ ID ของคุณเอง)
4. บันทึกไฟล์

### ขั้นตอนที่ 5: รีสตาร์ท Development Server

```bash
# หยุด server ปัจจุบัน (Ctrl+C)
# แล้วรันใหม่
npm run dev
```

---

## ✅ ตรวจสอบการตั้งค่า

หลังจากตั้งค่าแล้ว ระบบจะสามารถ:
- ✅ ตรวจสอบ Brand Search Position (อันดับการค้นหาแบรนด์)
- ✅ ตรวจสอบ Keyword Ranking (อันดับคีย์เวิร์ด)
- ✅ วิเคราะห์ Competitor Position (ตำแหน่งคู่แข่ง)
- ✅ ใช้ใน Keyword Discovery Module

---

## 💰 ราคาและ Quota

### Free Tier (100 queries/day)
- **Daily quota**: 100 queries ต่อวัน
- **Cost**: ฟรี
- เหมาะสำหรับ: การทดสอบและใช้งานส่วนตัว

### Paid Tier
- **$5 per 1,000 queries** (หลังจาก 100 queries/day)
- เหมาะสำหรับ: การใช้งานจริงใน production

### ตรวจสอบ Quota
- ไปที่ [Google Cloud Console → APIs & Services → Dashboard](https://console.cloud.google.com/apis/dashboard)
- เลือก "Custom Search API"
- ดู "Quotas" เพื่อตรวจสอบการใช้งาน

---

## 🔧 Troubleshooting

### ปัญหา: API Key ไม่ทำงาน

**แก้ไข**:
1. ตรวจสอบว่า API Key ถูกจำกัดให้ใช้กับ Custom Search API เท่านั้น
2. ตรวจสอบว่า Custom Search API ถูกเปิดใช้งานแล้ว
3. ตรวจสอบว่า API Key ถูกคัดลอกมาครบถ้วน (ไม่มีช่องว่าง)

### ปัญหา: "API key not valid" error

**แก้ไข**:
1. ตรวจสอบว่า API Key ถูกต้องใน `.env`
2. ตรวจสอบว่า API Key ไม่ถูก disable ใน Google Cloud Console
3. ตรวจสอบว่า Custom Search API ถูก enable แล้ว

### ปัญหา: "Search engine ID not found" error

**แก้ไข**:
1. ตรวจสอบว่า Search Engine ID ถูกต้องใน `.env`
2. ตรวจสอบว่า Custom Search Engine ถูกสร้างแล้วและยัง active อยู่
3. ไปที่ [Programmable Search Engine](https://programmablesearchengine.google.com/) เพื่อตรวจสอบ

### ปัญหา: Quota exceeded (429 error)

**แก้ไข**:
1. รอให้ quota reset (ทุกวันเวลา 00:00 UTC)
2. หรือ upgrade เป็น paid tier
3. ตรวจสอบการใช้งานใน [Google Cloud Console](https://console.cloud.google.com/apis/dashboard)

---

## 📝 ตัวอย่างการใช้งาน

หลังจากตั้งค่าแล้ว ระบบจะใช้ Google Custom Search API สำหรับ:

1. **Brand Search Ranking** (5 คะแนน):
   - ตรวจสอบว่าชื่อแบรนด์ติดอันดับ #1 ใน Google หรือไม่
   - ใช้ใน Brand Ranking pillar

2. **Keyword Discovery**:
   - ตรวจสอบ ranking ของ keywords ที่ Gemini แนะนำ
   - ใช้ใน Keyword Visibility pillar

3. **Competitor Analysis**:
   - เปรียบเทียบตำแหน่งกับคู่แข่ง
   - ใช้ในการวิเคราะห์เปรียบเทียบ

---

## 🔗 ลิงก์ที่เกี่ยวข้อง

- [Google Custom Search API Documentation](https://developers.google.com/custom-search/v1/overview)
- [Programmable Search Engine](https://programmablesearchengine.google.com/)
- [Google Cloud Console](https://console.cloud.google.com/)
- [API Credentials](https://console.cloud.google.com/apis/credentials)

---

## ⚠️ หมายเหตุสำคัญ

1. **API Key Security**: 
   - อย่า commit API Key ลงใน Git
   - ใช้ `.env` และเพิ่ม `.env` ใน `.gitignore`
   - ใน production ใช้ environment variables ของ hosting platform

2. **Quota Management**:
   - ตรวจสอบ quota เป็นประจำ
   - ใช้ caching เพื่อลด API calls
   - พิจารณา upgrade ถ้าต้องการใช้งานมาก

3. **Search Engine Configuration**:
   - สำหรับการค้นหาแบบทั่วไป ใช้ `*` ใน "Sites to search"
   - สำหรับการค้นหาเฉพาะ domain ระบุ domain เฉพาะ

---

## ✅ Checklist

- [ ] สร้าง Custom Search Engine และได้ Search Engine ID (เช่น `978788761c749457a`)
- [ ] เปิดใช้งาน Custom Search API ใน Google Cloud Console
- [ ] ตรวจสอบว่า API Key รองรับทั้ง Custom Search API และ PageSpeed Insights API
- [ ] เพิ่ม `GOOGLE_CUSTOM_SEARCH_API_KEY` ใน `.env` (ใช้ตัวเดียวกับ `GOOGLE_PAGESPEED_API_KEY` หรือแยกกัน)
- [ ] เพิ่ม `GOOGLE_CUSTOM_SEARCH_ENGINE_ID` ใน `.env` (เช่น `978788761c749457a`)
- [ ] รีสตาร์ท development server
- [ ] ทดสอบด้วยการรัน analysis

---

## 💡 สรุปสั้นๆ

### สำหรับผู้ที่ใช้ Google PageSpeed API อยู่แล้ว:

```env
# ใช้ API Key เดียวกัน
GOOGLE_PAGESPEED_API_KEY=AIzaSy...your-existing-key
GOOGLE_CUSTOM_SEARCH_API_KEY=AIzaSy...your-existing-key  # ใช้ตัวเดียวกัน
GOOGLE_CUSTOM_SEARCH_ENGINE_ID=978788761c749457a
```

**สิ่งที่ต้องทำ**:
1. ตรวจสอบว่า API Key รองรับ **Custom Search API** (เพิ่มใน API restrictions)
2. เพิ่ม `GOOGLE_CUSTOM_SEARCH_ENGINE_ID` ใน `.env`
3. ตั้งค่า `GOOGLE_CUSTOM_SEARCH_API_KEY` ให้ใช้ค่าเดียวกับ `GOOGLE_PAGESPEED_API_KEY`

---

**พร้อมใช้งานแล้ว!** 🎉

